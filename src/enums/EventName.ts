export enum EventName {
  previousLevel = 'previouseLevel',
  nextLevel = 'nextLevel',
  setLevel = 'setLevel',
  reset = 'reset',
  hint = 'hint',
  generateSubject = 'generateSubject',
  loose = 'loose',
  win = 'win',
  validate = 'validate',
}
