export enum EventName {
  reset = 'reset',
  hint = 'hint',
  generateSubject = 'generateSubject',
  loose = 'loose',
  win = 'win',
  validate = 'validate',
  onWin = 'onWin',
  resize = 'resize',
}
